import React from "react";

const Linkaccent = (props) => {
  return (
    <span className="cursor-pointer text-sm text-primary font-semibold">
      {props.text}
    </span>
  );
};

export default Linkaccent;
